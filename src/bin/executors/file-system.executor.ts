import { IExecutor } from "../interfaces/executor.interface";
import { BaseExecutor } from "./base.executor";

class FileSystemExecutor extends BaseExecutor  implements IExecutor {

}

export { FileSystemExecutor }